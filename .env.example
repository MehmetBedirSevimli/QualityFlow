# SQL Server
SQLSERVER_DSN=DRIVER={ODBC Driver 18 for SQL Server};SERVER=localhost,1433;DATABASE=QualityFlowDB;UID=sa;PWD=REPLACE_ME;TrustServerCertificate=yes;

# PLC bağlantı ayarları
PLC_IP=192.168.0.1
PLC_RACK=0
PLC_SLOT=1

# Collector tarafindan alinacak maksimum kayit sayisi
MAX_RECORDS=50000

# API (ileride)
API_HOST=0.0.0.0
API_PORT=8000
JWT_SECRET=REPLACE_ME
JWT_ALGO=HS256

# Dashboard (ileride)
STREAMLIT_PORT=8501
